name: electron
region: eu-central-1
language: ts
cloudProvider: genezio
scripts:
  preFrontendDeploy: npm install && npm run build
frontend:
  path: ./build
  subdomain: cyan-tremendous-minnow
packageManager: npm
